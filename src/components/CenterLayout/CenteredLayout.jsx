import React from "react";
import "./CenteredLayout.css"; // Import the styles

const CenteredLayout = ({ children }) => {
  return <div className="centered-container">{children}</div>;
};

export default CenteredLayout;
                     